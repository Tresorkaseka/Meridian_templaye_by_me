# Tech Context

## Technologies Used
- Frontend Framework: React 18+
- Language: TypeScript 5+
- Build Tool: Vite
- CSS Framework: Tailwind CSS v3 (checking package.json before deciding on v4)
- Animation: Framer Motion
- Icons: Phosphor Icons (`@phosphor-icons/react`)

## Development Setup
- Project root at `C:\Users\HP\.gemini\antigravity\scratch\saas-landing-page`.
- `npm run dev` for local hosting.
- `npm run build` for production checks.

## Technical Constraints
- No usage of arbitrary z-index piling.
- Animations must use hardware-accelerated properties (`transform`, `opacity`).
- No generic AI data (use realistic, organic placeholders).
